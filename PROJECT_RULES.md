# PROJECT RULES · 최우선 제품 규칙
버전 cm-2026-09-24. 최신 사용자 명시 요구 > 본 규칙 > product-decisions > 계약/지표 > UI 명세 > 역할/스킬 > 과거 문서.
기존 SQL와 README의 phone-only, manager-private, yearly-only는 **현재 구현 정본이 아닌 과거 v1**이다.
변경 근거 없이 예전 제한으로 요구사항을 되돌리지 않는다.

## 고정된 제품 요구
1. 공개 담당자명은 전체 성명이다. 기관과 함께 표시한다. 이름만으로 동명이인을 합치지 않는다.
2. 차량은 비공개 원번호로 집계한 후 표시만 마스킹한다. 지역명(짧은 형태, 예: 경기)은 그대로 표시하고, 위치를 셀 때만
   건너뛴다. 지역명 뒤 문자열의 1-based 2/4/6번째 문자 각각을 '*'로 바꾼다(경기76자3623 → 경기7*자*6*3). 한글도 한 글자다. 원번호·해시·전역 차량 연결키는 공개 API/JSON/DOM/URL/CSV에 금지한다.
3. 공개 좌표는 입력 좌표 그대로다. privacy 목적의 격자화·무작위 이동·일괄 절삭 금지.
4. 표본 1건도 수집·공개한다. n<3 등의 비공개·차트 삭제 기본값 금지. '표본 1건'을 보일 수는 있다.
5. 대한민국 전국, 기간은 사용자가 날짜 범위로 선택한다. 월/연도만 가능한 UI로 축소 금지.
6. 날짜 기준은 지표별이다. 신고량=신고일, 처리·처분량=처리완료일. 분모·결측·비교기간을 설명한다.
7. Google 계정으로 기존 앱/서버가 기여한다. 자동 업로드 02~03시 KST 목표와 갱신 토큰 설계는 upstream 계약에 보존한다.
   이 공개 지도 화면의 열람에는 로그인을 요구하지 않는다.
8. Sol과 Muse가 공동 구현하고 Muse가 실제 브라우저 검수한다. 모델 ID를 임의로 추정하지 않는다.

## 보안·정확성
- GitHub Secrets에 넣었다고 번들에 삽입한 값이 비밀이 되지 않는다. VITE_*에는 공개 설정만.
- Supabase service_role/secret는 읽기 전용 키가 아니다. CI는 SELECT 전용 export role 또는 제한된 export endpoint 사용.
- 담당자/기관 '처리결과 비교'이지 인물의 우열·능력·공정성 평가가 아니다. 합성 성과점수/등급/우수·무능 판정 금지.
- 신고 건수는 위반 확정 건수도 실제 지역 발생률도 아니다. 표본 밖 모집단을 추정해 말하지 않는다.
- 마스킹·실명 동의가 법적 익명성을 자동으로 보장한다고 쓰지 않는다. 공개 범위는 사용자 제품 결정이고 운영 검토는 별개다.
- 기존 marginal breakdown을 곱하거나 비례 배분해 담당자별 상태 교차통계를 만들지 않는다.
- 월별 TOP5의 합으로 임의 기간 TOP5를 만들지 않는다. 전체 private 후보 집합에서 다시 집계한다.
- 없는 완료일을 업로드일/신고일로 대체하지 않는다. 실데이터 실패 때 demo를 정상 페이지로 몰래 대체하지 않는다.
- 운영에서 공개할 파일은 build allowlist만 포함. docs, tests, raw data, logs는 Pages artifact에서 제외.

## 작업 범위
이 패키지 배치와 레포 로컬 구현·테스트는 허용한다. upstream 두 레포 수정, 운영 SQL 실행, 클라우드 계정 설정,
push, release, Pages 실제 배포는 명시적 승인을 별도로 받는다. 준비 가능한 로컬 작업을 환경 대기만으로 중단하지 않는다.
