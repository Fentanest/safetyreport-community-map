# GPT-6-Sol · 7일 감사 수정 6차 재검증

## 읽은 범위와 실행 증거

PC `1997065..9f1adc6`의 변경 7개 파일(`start.py`, `services/crawl_manager.py`, `services/crawl_control.py`, 새 `services/crawl_queue_report.py`, `tests/test_storage_exchange.py`, 계약 문서, changelog) 전체 diff와 저장 계층·목록 API·호출자를 읽었다. 지도 `b911b41`은 이번 회차 문서·증거만 추가했다. 모바일 제품 `2debce44`, 지도 제품 `e284c86`, 인증 제품 `1dddcc4`는 이전 재검증 뒤 변경되지 않았다. 대상 worktree·branch·스택 DB는 변경하지 않았고 직접 실행은 `/tmp`의 임시 데이터 폴더에서 했다.

`evidence/2026-09-26-audit6/`의 명령, 후보, 본문, 종료 코드를 확인했다. PC `9f1adc6`: 단위 **396 OK(4 skip)**, 실스택 1 OK, 브라우저 **128 passed**, 서버↔모바일 왕복 `diff_count=0`. 여섯 음성 대조는 각각 수정 전 `1997065`의 별도 dirty 트리에서 실행됐고 모두 목표 단언에서 실패했다. 두 R5-01 대조는 보고 없는 번호 삭제/실제 자식의 보고 누락, 두 R5-02 대조는 완료 훅 뒤 재시작/재시도 타이머, R5-03은 알림 예외 뒤 예약 잔류, R5-04는 준비 실패 뒤 파일 잔류를 검출한다. import 실패나 검사 환경 오류가 실패 원인은 아니다. 새 회귀 테스트 4건도 임시 데이터 폴더에서 따로 실행해 **4/4 통과**를 확인했다.

현재 지도 branch의 추적 증거 **139개 파일**(이번 로그 10개 포함)을 값 출력 없이 재검사했다. 환경 전체 덤프, 앞서 문제가 된 세션 표식, 인증 헤더·JWT·비밀키 형식의 일치 건수는 모두 **0**이다. 기존 도달 불가능한 로컬 commit 객체와 외부 복제본·당시 자격 정보의 폐기 여부는 확인할 수 없다. 이 보고서와 재현 출력에는 환경 변수 값이나 토큰을 싣지 않았다.

## 5차 finding 재판정

| ID | 판정 | 직접 확인한 코드·검사 경계 |
|---|---|---|
| **R5-01** | **부분 · 높음 잔여** | `start.py:440-444,468-473`은 `SaveResult.saved`가 참인 ID를 큐 원문 번호로 되돌려 결과 파일에 쓰고, 부모 `crawl_manager.py:402-418`은 종료 코드와 무관하게 보고된 번호만 뺀다. 로그인 실패나 읽지 않고 0 종료한 자식은 보고가 없어서 번호가 남았다. 하지만 목록 탐색 실패·불완전 탐색도 `not_found`로 보고되고, 모호한 부분 번호는 첫 ID에 임의 연결된다(R6-01·02). |
| **R5-02** | **부분 · 중간 잔여** | `crawl_control.py:184-198,241-255`의 삽입 뒤 재시작 요청과 `crawl_manager.py:236-252,412-414`의 지수 간격 타이머로 기존의 단순 완료 훅 경쟁과 자식 실패 뒤 무트리거 경로는 개선됐다. 그러나 게이트가 타이머 호출을 막으면 재예약 없이 끝나며, 프로세스 재시작 때 내구 큐의 타이머도 복원되지 않는다(R6-03). |
| **R5-03** | **닫힘 — 지적한 알림 실패 경로** | `crawl_manager.py:420-435`가 감시 스레드를 먼저 시작하고 알림 예외를 격리한다. 알림 실패를 다시 주입해 자식 종료 뒤 예약 0을 확인했다. 다른 보고 파일 손상 경로는 R6-05. |
| **R5-04** | **닫힘** | `crawl_manager.py:383-393,438-442`가 `prepare`·Popen 실패 시 실행별 큐·보고·임시 파일을 지운다. 로그 준비 실패를 다시 주입해 자식 시작 0, 내구 번호 유지, 예약 0, 실행별 파일 0을 확인했다. |
| **R4-01** | **부분 · 높음 잔여** | 읽기 전 종료나 0 종료만으로는 번호를 지우지 않는 계약으로 개선됐다. 그러나 `not_found` 오판과 모호한 부분 번호의 처리 완료 보고가 아직 번호를 영구 제거할 수 있다(R6-01·02). |
| **SOL-04** | **부분** | 복원 쓰기 장벽·중지 대기·세대 검사와 이번 자식 보고 경로는 유지된다. 자동 큐의 거짓 완료 보고(R6-01·02), 게이트 차단 뒤 재시도 소멸(R6-03)을 해결해야 복원 뒤 남은 번호의 처리 보장을 닫을 수 있다. |

## `/tmp` 재현과 보고·재시도 경계

| 조건 | 새 코드에서 관측한 결과 |
|---|---|
| 큐 파일을 읽지 않고 자식이 **0 종료** | 보고 파일이 없으므로 부모 큐의 번호가 유지되고 재시도 타이머가 생긴다. 종료 코드만 믿던 R5-01 경로는 차단. |
| `start.main()` 직접 로그인·대체 로그인 모두 실패 | 자식은 정상 반환하지만 보고가 없고 부모 번호는 유지. 새 실제 자식 테스트도 이 경로를 통과. |
| 완료 훅이 지난 뒤 사용자 번호 추가 | 응답 `queued`, 별도 launch 요청으로 한 번 시작됨. 해당 테스트의 수정 전 음성 대조는 시작 횟수 단언에서 실패. |
| WebSocket 알림 예외 | 자식 감시가 계속되어 보고된 번호 정리·예약 해제, 시작 결과 참. |
| `prepare()` 중 로그 준비 실패 | 내구 번호는 남고 실행별 큐 파일·예약은 남지 않음. |
| 결과 보고 파일 원자 교체 실패 | 자식은 오류를 기록하고 보고 없음. 부모는 번호를 뺄 수 없어 다시 시도한다. 임시 보고 파일은 실행 종료 정리 대상이다. |
| 미확인 번호 검색 첫 페이지의 일시 실패 | 번호가 `not_found`에 기록됨: **완료로 오인**(R6-01). |
| 실패 번호의 타이머가 발화할 때 게이트 거부 | 내구 번호 1개는 남지만 타이머가 `None`으로 끝남(R6-03). |
| 대기 시작 요청 25회가 느린 launch를 만남 | `crawl-pending-request` 스레드 25개가 동시에 생김(R6-04). |

저장 보고의 정상 경로는 `reports_repo.save_crawled()`가 한 건의 DB 트랜잭션을 마치고 `saved`를 증가시킨 다음에만 ID를 보고한다(`core/storage/reports_repo.py:238-260,275-301`). 동일 ID를 두 번 받거나 큐에 같은 번호가 두 번 있으면 보고는 집합으로 중복 제거된다(`crawl_queue_report.py:16-24`). 같은 ID를 가리키는 두 **정확한 별칭**은 한 저장이 성공하면 둘 다 완료로 표시되며, 실제로 같은 신고를 뜻하는 경우에는 타당하다. 이와 달리 모호한 부분 번호는 어느 신고인지 확정되지 않았는데도 한 ID를 선택한다(R6-02). 저장 실패·중단 뒤 보고되지 않은 번호는 유지되지만, 보고 누락 시 재처리로 중복 작업은 가능하다.

## 새 결함·회귀

| ID·심각도 | 파일:줄 · 재현/원인 | 실제 영향 · 수정 제안 · 회귀 시험 |
|---|---|---|
| **R6-01 · 높음** | PC `start.py:380-425,442-444`, `services/crawl_queue_report.py:27-38`. 미확인 번호 탐색에서 `crawl_titles()`가 예외를 내거나 빈 목록을 반환하면 곧바로 반복을 중단하지만 `missing_rnums`를 그대로 `not_found`에 넣는다. 실제 목록 API도 첫 페이지 네트워크 실패·응답 오류를 `([], 0)`으로 반환한다(`core/crawler/crawltitle_api.py:83-101`). `/tmp`에서 일시 실패를 주입하자 미확인 번호 1개가 완료 보고 1개가 됐다. 100페이지 상한에 도달했어도 같은 오판이 가능하다. | 일시 장애나 탐색 범위 부족을 '없는 신고'로 확정해 내구 큐에서 영구 삭제한다. 모든 필요한 페이지의 **성공적인** 검색이 입증될 때만 `not_found`를 보고하고, 실패·상한 도달은 미완료로 남겨 재시도한다. 첫 페이지 실패, 중간 페이지 실패, 빈 정상 결과, 100페이지 초과 각각을 부모 큐 정리까지 잇는 시험이 필요하다. |
| **R6-02 · 높음** | PC `start.py:99-108,119-131,400-407,440-444`. 초기 해석은 부분 일치가 두 건 이상이면 거부하지만, 목록 탐색 뒤 재검색은 같은 `LIKE` 질의의 `.scalar()` **첫 ID**를 선택한다. 임시 fixture의 한 부분 번호가 24개 신고와 일치했으며 초기 해석은 미확인으로 두지만 후속 질의는 임의 ID 하나를 선택했다. 이를 저장하면 그 부분 번호가 `processed`에 들어가 큐에서 빠진다. | 사용자가 의도한 신고와 다른 상세를 수집하고 요청 번호를 완료 처리할 수 있다. 후속 검색에도 정확 일치→접두어 정확 일치→유일한 부분 일치 규칙을 그대로 적용하고, 복수 일치는 미해결로 남겨 사용자에게 모호함을 알린다. 두 일치 건의 순서를 바꾼 fixture와 같은 ID/번호 중복을 포함해 보고·부모 ack를 시험한다. |
| **R6-03 · 중간** | PC `crawl_manager.py:236-252,354-359`. 타이머는 발화 즉시 `_retry_timer=None`으로 지운 뒤 launch를 한 번 부른다. 게이트가 거부해 `launch_pending_crawl()`이 `False`로 끝나도 다시 예약하지 않는다. `/tmp`에서 게이트 거부 후 대기 번호 1개·타이머 없음 확인. `:142-143`은 시작 때 자동 재개하지 않는다고 명시해 프로세스 재시작으로도 예약 타이머가 사라진다. | R5-02의 재시도 보장이 게이트 장애·재시작 뒤 다시 무기한 대기로 돌아간다. 차단 사유를 존중하며 유한한 지수 간격으로 다시 확인하거나 게이트 상태 변화·서버 기동 이벤트에서 한 작업만 재예약한다. 타이머 발화 중 게이트 거부→허용, 프로세스 재시작, 종료 시 타이머 정리를 시험한다. 허용 검사 통과 뒤 영구 실패는 30분 간격으로 무한 재시도하며 현재 시도 상한·사용자 알림은 없다. |
| **R6-04 · 중간** | PC `crawl_manager.py:232-234`, `crawl_control.py:184-198,241-255`. 대기 번호 추가마다 같은 번호의 중복 요청까지 무조건 daemon 스레드를 하나씩 시작한다. `/tmp`에서 launch를 잠시 막고 25번 요청하자 스레드 25개가 대기했다. 각 스레드는 단일 launch 잠금 아래 최대 10초의 게이트 네트워크 확인을 순차 실행할 수 있다. | 요청 집중 시 스레드·메모리 증가와 자동 큐 지연, 스레드 생성 실패 시 `queued` 응답 대신 예외가 생길 수 있다. 단일 작업자/예약 플래그로 요청을 합치고 실행 중이면 검사를 생략한다. 같은 번호 및 서로 다른 번호를 병렬 삽입해 작업자 상한과 최종 처리 여부를 시험한다. |
| **R6-05 · 중간** | PC `services/crawl_queue_report.py:27-38`은 JSON 구문 오류는 처리하지만 `{"processed": 7}`처럼 유효한 JSON의 잘못된 필드 타입에서 `TypeError`를 낸다. `/tmp`에서 이 입력의 `read()`가 `TypeError`인 것을 확인했다. `crawl_manager.py:396-418`은 보고 읽기를 감시 스레드의 정리 `finally`보다 앞에서 호출한다. | 손상·버전 불일치 보고 파일 하나가 감시 스레드를 종료시켜 예약과 실행별 파일을 영구 잔류시킬 수 있다. 필드 타입을 검증하고 잘못된 보고는 빈 보고로 취급하며, 감시 전체를 예약 해제·파일 정리 `finally`로 감싼다. 잘못된 필드 타입·부분 JSON·읽기 예외 뒤에도 대기 번호가 보이는지 시험한다. |

PyInstaller 경로는 `main.py:39-42`의 crawl 진입과 새 모듈에 대한 정적 `from services import crawl_queue_report`를 확인했다. 동적 모듈 이름 조립은 없어서 정적 분석 대상이지만, 이 환경에는 PyInstaller가 없고 audit6에 frozen 산출물 검사가 없어 **실제 번들 포함은 미검증**이다. 재시도 타이머는 daemon이라 프로세스 종료를 막지 않지만 별도 shutdown 취소·다음 기동 재예약은 없다. 오래 남는 번호의 반복 요청 횟수·운영 비용도 측정하지 않았다.

## SOL-01~10 최종 상태

| ID | 상태 | 근거·한계 |
|---|---|---|
| SOL-01 | 닫힘 | ACK 코드 변경 없음; 이전 PC·모바일 검증과 이번 PC 단위·실스택 통과. |
| SOL-02 | 닫힘 | 혼합 DB의 미지 열 거부·정상 복원 판정 유지; 이번 왕복 차이 0. |
| SOL-03 | 닫힘 | NULL/빈 값 변환 코드 변경 없음; 이번 왕복 차이 0. |
| SOL-04 | **부분** | 복원 장벽·세대 검사 유지; R6-01·02의 거짓 완료와 R6-03의 재시도 소멸 잔여. |
| SOL-05 | 닫힘 | 모바일 되돌리기·모드 상태 제품 코드 변경 없음. |
| SOL-06 | 닫힘 | 지도 `point_count`·완료 전용 빈 결과 제품 코드 변경 없음; 완료 전용 실제 브라우저는 미검증. |
| SOL-07 | 닫힘 | 결측 좌표 집계 변경 없음. |
| SOL-08 | 닫힘 | entities API 하위호환 변경 없음; 대량 요청 비용은 미측정. |
| SOL-09 | 닫힘 | compose 명시 경로 변경 없음. |
| SOL-10 | 닫힘 | 요청 body 제한 변경 없음. |

## 결론

**수정 후 재검토.** 종료 코드만 믿던 삭제, 로그인 실패, 완료 훅 경합, 알림 예외, 준비 실패는 개선됐다. 그러나 검색 실패를 `not_found`로 확정하는 R6-01과 모호한 부분 번호를 임의 ID로 완료시키는 R6-02는 **높음** 데이터 손실 위험이므로 R5-01·R4-01·SOL-04를 닫을 수 없다. 게이트 차단 뒤 재시도 소멸, 무제한 시작 스레드, 손상 보고의 예약 누수도 수정과 결함 검출력이 있는 회귀 시험이 필요하다.
