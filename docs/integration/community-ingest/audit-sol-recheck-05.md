# GPT-6-Sol · 7일 감사 수정 5차 재검증

## 범위와 증거

PC `cb1305d..1997065`의 변경 6개 파일 전체 diff와 `crawl_manager`·`crawl_control`·`start.py`의 실제 호출 경로를 읽었다. 지도는 `804a30b..bfc9edd`의 증거 수정, `bfc9edd..8820d9f`의 문서·증거 diff를 확인했다. 모바일 제품 HEAD `2debce44`, 지도 제품 HEAD `e284c86`, 인증 제품 HEAD `1dddcc4`에는 이번 회차 제품 코드 변경이 없다. 앞선 재검증의 SOL-01~10 근거도 대조했다. 대상 worktree·branch·스택 DB는 변경하지 않았고 독립 재현은 `/tmp`의 임시 폴더와 가짜 자식 프로세스를 썼다.

`evidence/2026-09-26-audit5/`의 명령, 후보 SHA, 본문, 종료 코드를 읽었다. PC `1997065` 단위 검사는 **392 OK(4 skip)**, 실스택 1 OK, 브라우저 **128 passed**, 서버↔모바일 왕복 `diff_count=0`이다. 네 음성 대조는 각각 수정 전 `cb1305d`의 별도 dirty 작업 트리에서 실행됐고, 모두 목표 단언에서 종료 코드 1로 실패했다. `neg-r4-01-remove-on-start.log`는 자식 실패 뒤 큐 보존 단언, `neg-r4-02-no-launch-lock.log`는 같은 번호 중복 실행 횟수, `neg-r4-02-user-log-before-start.log`는 실행 중인 로그의 머리말, `neg-r4-03-no-generation.log`는 복원 뒤 시작 금지 단언에서 실패했다. 다른 준비·import 오류가 검출력을 대신한 것은 아니다. 다만 성공 테스트의 가짜 자식은 실제 처리 결과를 증명하지 않는다.

지도 branch의 추적된 증거 129개 파일을 바이트 패턴으로 다시 검사한 결과, 앞서 노출된 세션 값의 환경 변수 이름, 환경 전체 출력 표시 및 관련 세션 변수 표시가 각각 0건이었다. `git for-each-ref --contains 804a30b` 결과는 비었고 현재 ref·reflog에서 옛 commit을 찾지 못했다. 옛 commit 객체는 로컬 객체 저장소에 아직 존재한다. 외부 복제본의 삭제와 당시 자격 정보의 폐기 여부는 확인할 수 없다. 이 보고서에는 환경 변수 값이나 토큰을 싣지 않는다.

## R4 항목과 SOL-04 재판정

| 항목 | 판정 | 코드·검사 근거 |
|---|---|---|
| **R4-01** | **부분 · 높음 잔여** | PC `services/crawl_manager.py:327-383`은 자식이 살아 있는 동안 번호를 예약해 내구 큐에 남기고, 종료 코드 0이면 제거하며 그 외에는 예약을 푼다. `tests/test_storage_exchange.py:628-645`와 음성 대조는 시작 직후 선제 삭제를 검출한다. 그러나 종료 코드 0이 해당 번호의 읽기·처리·저장을 뜻하지 않는다. 새 R5-01 참조. |
| **R4-02** | **닫힘** | PC `crawl_manager.py:308-356`의 launch 잠금과 예약, `crawl_control.py:188-195`의 시작 확정 후 로그 준비가 동시 중복·로그 덮어쓰기를 막는다. `test_storage_exchange.py:646-697` 두 동시 launch 시험과 두 음성 대조가 목표 경쟁을 검출한다. `/tmp`에서도 동시 launch 두 개에 시작 1회·자식 입력 1회, 사용자 시작 경쟁에 기존 로그 보존·응답 `queued`를 확인했다. |
| **R4-03** | **닫힘** | PC `crawl_manager.py:309-340`에서 검사 전 복원 세대를 읽어 `start_crawl()`에 전달하고, `:72-103`에서 상태 잠금 아래 다시 비교한다. 자동 시작과 사용자 시작 모두 `test_storage_exchange.py:698` 이하에서 검사→복원→시작 순서를 강제한다. 음성 대조도 시작 금지 단언에서 실패했다. `/tmp`에서는 복원 후 자식 시작 0회, 큐 유지였다. |
| **R4-04** | **닫힘 — 현재 병합 branch 기준** | 지도 `bfc9edd`가 민감한 음성 대조 출력 자체를 제거했다. 현재 ref에 옛 commit이 없고 추적된 전체 증거 재검사에서도 해당 출력 형태가 0건이다. 로컬의 도달 불가능한 객체와 외부 복제본·자격 정보 폐기는 별도 운영 한계다. |
| **SOL-04** | **부분** | 기존 복원 쓰기 장벽·중지 대기·세대 경쟁은 유지되며 R4-03도 닫혔다. 하지만 자동 큐의 처리 확인 없는 성공 제거(R5-01)와 실패 뒤 대기 지속(R5-02) 때문에 복원 뒤 남은 번호가 안전하게 처리된다는 계약까지 닫히지 않는다. |

## 요청한 `/tmp` 재현과 새 실패 경로

| 조건 | 새 코드의 관측값 | 의미 |
|---|---|---|
| 자식이 큐 파일을 읽기 전에 비정상 종료 | 실행 중 내구 큐에 번호 1개, 예약 때문에 대기 수 0, 실행별 큐 파일 존재. 종료 코드 1 뒤 번호 1개·대기 수 1, 실행별 파일 삭제. | R4-01의 **비정상 종료** 선제 삭제는 해결됐다. |
| 동시 자동 launch 두 개, 빠른 자식 종료 | 반환 `[False, True]`, 시작 1회, 같은 번호 전달 1회. | R4-02 중복 방지 확인. |
| 허용 검사 뒤 복원, 시작 전 세대 변경 | 반환 `False`, 시작 0회, 복원 세대 증가, 번호는 큐에 유지. | R4-03 시점 경쟁 차단 확인. |
| 자동 시작 중 사용자 시작 경쟁 | 사용자 응답 `queued`, 실제 시작 1회, 실행 중인 로그 머리말 보존, 사용자 번호는 내구 큐에 유지. | R4-02 로그 손상 차단. 이 응답 뒤 재실행 보장은 R5-02 참조. |

같은 임시 재현에서 실패한 자식이 큐 파일을 읽지 않은 채 **종료 코드 0**을 돌려주자 번호가 큐에서 사라졌다. 별도로 `start.main()`의 로그인·대체 로그인 실패를 주입했을 때도 오류를 기록하고 정상 반환했다. 따라서 가짜 자식의 0 종료만 가정한 문제가 아니라 실제 자식 코드의 실패 경로와 연결된다. 재현 출력에는 환경 값이나 토큰을 넣지 않았다.

## 새 결함·회귀

| ID·심각도 | 파일:줄 · 재현/원인 | 실제 영향 · 수정 제안 · 필요한 회귀 시험 |
|---|---|---|
| **R5-01 · 높음** | PC `services/crawl_manager.py:365-378`은 자식 종료 코드 0만으로 맡긴 번호 전체를 내구 큐에서 제거한다. 하지만 `start.py:535-575`는 직접 로그인과 대체 로그인이 모두 실패해도 오류를 기록하고 정상 반환하며, `:580-587`의 후처리 오류도 삼킨다. `:393-405`에서는 찾지 못한 신고번호나 상세 대상 0건도 정상 반환하고, `:437-455`는 상세 스트림·저장 실패를 기록한 뒤 계속한다. 독립 재현에서 로그인 실패를 주입한 `start.main()`은 정상 반환했고, 0 종료한 읽지 않은 가짜 자식의 번호는 큐에서 사라졌다. | 로그인·검색·저장 실패한 신고가 처리된 것으로 확정되어 영구 누락될 수 있다. 자식이 번호별 읽기·처리·영속 저장 결과를 내구적으로 보고하고 확인된 번호만 ack한다. 최소한 치명적 실행 실패는 비정상 종료로 전달하고 0건·부분 실패의 의미를 정의한다. **실제 `start.main()` 실패 주입 + 부모 종료 훅**을 함께 지나 실패 번호가 재시작 뒤에도 남는 회귀 시험이 필요하다. 현재 테스트는 이 결함을 검출하지 못한다. |
| **R5-02 · 중간** | PC `crawl_manager.py:291-294,371-379`에서 비정상 종료 번호의 예약을 `run_after_crawl()`이 다음 대기 건수를 확인한 **뒤** 푼다. 그 번호만 있으면 후속 시작 트리거가 없다. `crawl_control.py:181-195`에서도 자동 완료 훅의 대기 검사 직후 사용자 요청이 시작 경쟁에서 밀리면 `queued`를 반환하지만 완료 훅은 이미 지나갔고 활성 크롤이 없다. 두 순서를 `/tmp`에서 강제하자 내구 번호는 남고 활성 자식·후속 launch는 없었다. | 실패 번호 또는 사용자에게 `queued`라고 알린 번호가 다음 별도 크롤·복원까지 무기한 대기할 수 있다. 예약 해제와 추가 삽입 뒤, 무한 즉시 재시도 대신 제한된 backoff/중앙 스케줄러에 작업을 통지하고 실제 예약 상태를 응답에 반영한다. 자식 실패 단독 번호와 완료 검사 직후 사용자 추가를 강제한 시험이 필요하다. |
| **R5-03 · 중간** | PC `crawl_manager.py:356-383`에서 Popen 성공 뒤 완료 감시 스레드를 만들기 **전에** WebSocket broadcast를 동기 호출한다. `/tmp`에서 broadcast 예외를 주입하자 launch가 예외로 끝나고 예약 1개·대기 수 0·내구 번호 1개가 남았다. 자식 종료 뒤에도 감시 훅이 없어서 예약이 풀리지 않았다. 스레드 시작 실패도 같은 구간이다. | 자식은 실행되나 예약이 영구 유지되어 해당 번호가 스케줄러에 보이지 않는다. 감시 훅을 먼저 보장하고 알림 예외를 격리하거나, 시작 후 후속 작업 실패 시 예약·프로세스 상태를 회수한다. broadcast·스레드 시작 실패를 각각 주입하고 종료 뒤 예약이 0인지 검사한다. |
| **R5-04 · 낮음** | PC `crawl_manager.py:327-347`의 `prepare()`가 실행별 큐 파일을 만든 뒤 로그 회전 등에 실패하면 예약은 풀지만 파일을 삭제하지 않는다. `/tmp`에서 로그 회전 실패를 주입하자 자식 시작 0·내구 번호 1·예약 0이지만 실행별 큐 파일 1개가 남았다. | 반복 실패 때 번호가 담긴 임시 파일이 누적된다. `prepare`/Popen 예외 처리에서 해당 파일을 삭제하고, 로그는 준비 성공이 확인된 뒤 교체한다. 파일 작성 후 로그 준비 실패와 Popen 실패를 검사한다. |

자동 launch 잠금은 `crawl_manager.py:308-319`의 게이트 검사와 그 안의 네트워크 재확인까지 잡는다. 계정 요청에는 10초 timeout이 있어 두 번째 자동 launch와 완료 훅의 후속 지오코딩이 늦어질 수 있다. 검사 동안 상태 잠금은 풀려 있고 사용자 시작은 별도 직렬화 경로를 사용하므로 이 코드만으로 순환 교착은 확인되지 않았다. 장애 시 장시간 지연의 상한과 동시 사용자 요청 지연은 실측하지 않았다. `prepare()` 예외는 잠금을 빠져나오며 예약은 풀리지만 R5-04의 파일 잔여와 재시도 트리거 부재가 남는다.

## SOL-01~10 최종 상태

| ID | 상태 | 근거·한계 |
|---|---|---|
| SOL-01 | 닫힘 | ACK 계약 코드 변경 없음; 이전 PC·모바일 검증과 이번 PC 단위·실스택 통과. |
| SOL-02 | 닫힘 | 혼합 DB의 미지 열 거부·정상 복원·유효한 음성 대조의 3차 판정 유지; 이번 왕복 차이 0. |
| SOL-03 | 닫힘 | NULL/빈 값 변환 코드 변경 없음; 이번 왕복 차이 0. |
| SOL-04 | **부분** | 복원 장벽·세대 경합은 해결; R5-01의 거짓 성공 제거와 R5-02의 대기 지속 잔여. |
| SOL-05 | 닫힘 | 모바일 되돌리기·모드 상태 코드 변경 없음; 이전 판정 유지. |
| SOL-06 | 닫힘 | 지도 `point_count`·완료 전용 빈 결과 판정 코드 변경 없음; 완료 전용 실브라우저는 여전히 미검증. |
| SOL-07 | 닫힘 | 결측 좌표 집계 변경 없음. |
| SOL-08 | 닫힘 | entities API 하위호환 코드 변경 없음; 대량 요청 비용은 미측정. |
| SOL-09 | 닫힘 | compose 명시 경로 변경 없음. |
| SOL-10 | 닫힘 | 요청 body 제한 변경 없음. |

## 결론

**수정 후 재검토.** R4-02·03·04는 현재 병합 후보에서 닫혔고 R4-01은 실패 종료까지는 개선됐으나, 실제 자식이 실패를 정상 종료로 숨기는 R5-01 **높음** 결함 때문에 SOL-04를 닫을 수 없다. 실패 후 번호의 무기한 대기와 예약 누수도 수정 및 회귀 시험이 필요하다. 모바일·지도·인증 제품의 이번 회차 독립 재실행은 하지 않았으며, 민감 증거의 외부 복제본·자격 정보 폐기 여부와 네트워크 지연 상한은 확인하지 못했다.
