# GPT-6-Sol · 병합 승인 뒤 추가 수정 10차 확인

## 읽은 범위·실행 근거

PC `61d2d41..2f925b5`의 `services/crawl_control.py`, `services/crawl_manager.py`, `main.py`, 회귀 테스트·계약 문서 diff와 직접 시작·중지·자동 대기열 완료 훅·기동 경로를 읽었다. 모바일 `0f344b91..83b57688`의 `crawl_screen.dart`, `crawl_unresolved.dart`, 테스트 diff와 상태 API 소비를 읽었다. 지도·인증 제품 코드는 이번 차수 변경 없음. 후보 worktree·목표 branch·스택 DB는 수정하지 않았고 직접 재현은 `/tmp` 데이터에서만 했다. 환경 변수 값·토큰은 출력하지 않았다.

`evidence/2026-09-26-audit10/`의 명령·candidate·dirty·종료 코드·실패 본문을 대조했다. PC 후보 `2f925b5` 단위 **409 OK(4 skip)**, 실스택 **1 OK**, 서버↔모바일 왕복 `diff_count=0`; 모바일 `83b57688` 단위 **528 passed(3 skipped)**, analyze **No issues found**, 실스택 **1 passed**다. 음성 대조 5건은 각각 고정 큐 이름, 기록 실패 때 삭제, 보고 없는 직접 실행의 미인계, 기동 미회수, 모바일 길이 비교라는 목표 단언에서 실패했다. PC 브라우저 전체 스모크는 **127 passed, 1 failed**(Firefox 운영 페이지 콘솔 오류)이고 같은 스펙 재실행은 **14/14 passed**다. 원인 기록은 외부 Pretendard CDN 폰트 다운로드 실패를 제시하며 이번 diff에는 UI/템플릿 변경이 없다. 다만 원본 trace 파일은 제공된 증거에서 열 수 없어 CDN 원인을 독립적으로 확정하지 못했고, 전체 128건의 깨끗한 재실행도 없다.

PC 새 표적 테스트 2건을 `/tmp`에서 별도 실행해 **2/2 통과**했다. 직접 시작 두 파일의 이름이 다르고, 보고 없는 번호·기록 실패 번호가 내구 큐에 남으며, 기동 회수가 표적 파일을 처리하는 조건을 검증한다. 모바일 `sameList` 구현은 번호·사유·시각을 순서대로 비교하며 같은 길이의 새 항목을 다르게 판정한다. 아래 새 재현은 표적 테스트가 만들지 않는 파일·중지·시작 경쟁 조건이다. 증거 디렉터리 185개 파일의 환경 전체 덤프, 자격 값 할당, Bearer/JWT/개인키 형태 검색 건수는 모두 **0**이었다(외부 복제본·옛 객체는 범위 밖).

## R9-01~04 재판정

| ID | 상태 | 파일:줄 · 판단 |
|---|---|---|
| **R9-01** | **닫힘 — 실행 중 보고 분리** | PC `crawl_control.py:100-108`은 직접 실행마다 UUID 큐 파일을 만들고 보고 경로도 달라진다. 새 테스트와 옛 고정 이름 음성 대조가 이를 확인한다. 다만 **시작이 확정되기 전에 만든 파일**을 실패·경쟁 분기에서 정리하지 않아 기동 때 다시 요청되는 문제는 R10-03으로 분리한다. |
| **R9-02** | **부분 · 높음 잔여** | PC `crawl_manager.py:233-258`은 미해결 기록 저장 실패 시 직접 번호를 내구 큐에 넘기고, 넘기지 못하면 실행 파일을 남긴다. 표적 테스트 통과. 그러나 큐 파일을 **읽지 못할 때 빈 요청으로 간주하고 파일을 삭제**하며(R10-01), 기동 회수는 관련 없는 접두어 파일까지 삭제한다(R10-01). 기존 고정 이름의 잔여 파일도 회수하지 않는다(R10-04). 따라서 안전한 복구 범위·보존 조건이 아직 충족되지 않았다. |
| **R9-03** | **닫힘** | 모바일 `crawl_screen.dart:253-262`, `crawl_unresolved.dart:4-47`은 길이가 같아도 번호·사유·시각 변경을 감지한다. 50개 중 한 항목만 교체하는 양성 테스트와 옛 길이 비교 음성 대조가 효과 있다. 실제 화면 위젯·실기기 표시 증거는 별도 운영 전 조건이다. |
| **R9-04** | **부분 · 높음 잔여** | 보고 없는 직접 실행의 번호를 내구 큐에 넘기고 재시도 타이머를 거는 기능은 동작한다(`crawl_manager.py:244-257`); 표적 테스트와 음성 대조도 이를 잡는다. 하지만 **사용자가 강제 중지한 직접 실행도 보고 없음으로 처리해 자동 재시작**한다(R10-02). 부분 번호의 전체 목록 조회 비용은 그대로다. |

## 새 회귀·재현

| ID·심각도 | 파일:줄 · `/tmp` 근거 | 실제 영향 · 수정 제안 · 필요한 회귀 시험 |
|---|---|---|
| **R10-01 · 높음** | PC `crawl_manager.py:267-283`은 `*_*.txt` 중 이름의 **접두어**만 확인한다. `/tmp`에 실행 파일이 아닌 `queue_notes.txt`와 `pending_queue_backup.txt`를 두고 `recover_leftover_queue_runs()`를 호출하자 **두 파일 모두 삭제**, 첫 파일의 메모는 신고번호 1건으로 대기열에 들어갔다. 별도 재현에서 정상 형식의 고유 직접 큐 파일 읽기에 `PermissionError`를 주입하자 `record_direct_queue_result()`는 `requested=[]`로 삼고(`:239-243`) 참을 반환하며 파일을 삭제했다(`:257`): 대기 번호 **0**, 원본 파일 **없음**. | 기동만으로 다른 파일을 삭제·임의 텍스트를 크롤 대기열로 넣거나, 일시적 읽기 오류의 실제 요청을 영구 소실시킨다. **정확한 생성 이름 형식**(허용 stem + 32자리 UUID, 자동 큐도 정해진 형식)과 파일 내용/상태를 확인하고, 읽기 오류는 실패로 돌려 원본을 보존해야 한다. 유사 이름의 메모·백업·legacy 파일, 손상/권한 오류, `.done` 유무와 재시작을 시험한다. |
| **R10-02 · 높음** | PC `crawl_control.py:302-309`의 강제 중지는 자식 프로세스를 끝내지만, 직접 시작 완료 훅(`:118-132`)은 중지 여부를 전달하지 않는다. `record_direct_queue_result()`는 보고 없는 요청을 대기 큐에 넣고 타이머를 건다(`crawl_manager.py:244-257`). `/tmp`의 가짜 프로세스를 `stop_crawl()`로 정상 중지한 뒤 완료 훅 경로를 실행하자 중지 결과 참인데 **대기 1·재시도 타이머 존재**였고, 타이머 화재 경로는 `launch_pending_crawl()`을 다시 호출했다. 대기 번호를 사용자가 취소하는 공개 경로도 없다. | 사용자의 **강제 중지**가 약 60초 뒤 자동 크롤로 뒤집히고 이후 반복될 수 있다. 중지 의도를 실행별로 기록해 해당 직접 실행의 자동 인계를 막거나, 사용자에게 보이는 명시적 재개 상태로 보존하되 타이머는 걸지 않아야 한다. 직접 실행 중지→완료 훅→타이머, 일반 네트워크 실패→재시도, 자동 대기열 중지의 정책을 각각 시험한다. |
| **R10-03 · 중간** | PC `crawl_control.py:175-186,216-225,273-283`은 고유 직접 큐 파일을 **프로세스 시작 전** 만들고, 시작이 경쟁에서 거짓을 돌려 대기열로 전환할 때 그 파일을 지우지 않는다. 기동 회수는 이를 미완료 직접 실행으로 다시 인계한다(`crawl_manager.py:267-283`). `/tmp`에서 `start_crawl=False`를 주입해 `enqueue_report`가 정상 `queued`를 반환하게 한 뒤 원래 대기 번호를 완료·제거하고 회수를 실행하자, 남은 고유 파일 때문에 대기열이 **0→1**로 돌아왔다. | 이미 완료된 신고를 다음 서버 기동 때 재크롤한다. 시작 실패·경쟁·복원 세대 변경 시 생성한 파일을 정리하거나, 실행이 확정된 파일만 회수 가능한 별도 마커를 남긴다. `enqueue_report(s)`와 모바일/웹 직접 시작의 실패·경쟁 후 완료→재시작 시험이 필요하다. |
| **R10-04 · 중간(업그레이드 회수 공백)** | PC `crawl_manager.py:267-270`은 UUID 접미어가 없는 이전 직접 실행 이름 `queue.txt`, `mobile_queue.txt`, `web_selected_queue.txt`를 선택하지 않는다. `/tmp`의 `mobile_queue.txt`를 둔 기동 회수에서 파일은 그대로, 대기열은 0이었다. | 이전 버전의 완료 훅이 중단된 직후 업데이트되면 미처리 직접 요청을 새 회수 코드가 찾아내지 못한다. 새 파일의 엄격한 UUID 패턴과 분리해 **정확한 세 legacy 이름만** 일회성으로 다루되 소유·내용 검증 후 보존/인계한다. 업데이트 직전 남은 고정 이름과 유사한 임의 파일의 경계를 시험한다. |

## 결론·남는 운영 전 조건

**감사 수정 병합 가능 판정을 유지할 수 없다 — 수정 후 재검토.** R9-01·03은 닫혔지만 새 기동 회수가 다른 파일을 삭제하고 읽기 실패 요청을 잃는 **R10-01 높음**, 사용자의 강제 중지를 자동 재시작으로 되돌리는 **R10-02 높음**이 있다. 9차에 승인했던 후보와 달리 이번 추가 수정은 병합 전 안전성 수정이 필요하다. R10-03·04도 재시작 회수의 정합성을 검증해야 한다.

위 결함을 고친 뒤에도 운영 전에는 **전체 목록 조회의 실제 시간·호출 수·메모리 측정(R7-04)**, **모바일 미해결 표시의 위젯·실기기 검증**, **구앱의 일반적인 400 오류 문구 안내**, **외부 CDN 실패가 포함된 브라우저 전체 스모크의 재확인**이 남는다.
