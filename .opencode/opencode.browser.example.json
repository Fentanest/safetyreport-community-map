{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "playwright": {
      "type": "local",
      "command": ["npx", "--no-install", "@playwright/mcp", "--isolated"],
      "enabled": true
    }
  }
}
